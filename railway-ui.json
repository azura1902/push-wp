{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "streamlit run ui/dashboard.py --server.port $PORT --server.address 0.0.0.0 --server.headless true",
    "healthcheckPath": "/_stcore/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
